use yew::prelude::*;

#[derive(PartialEq, Properties)]
pub struct TeamsProps {}

#[function_component]
pub fn Teams(props: &TeamsProps) -> Html {
    let TeamsProps {} = props;

    html! {
        <div></div>
    }
}