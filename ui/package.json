{
  "name": "tournoix-ui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx tailwindcss -i ./style.scss -o ./output.scss --minify",
    "copy-files" : "rm -rf ../backend/public/**.js ../backend/public/**.wasm ../backend/public/**.css && cp -r ./dist/. ../backend/public/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.3"
  }
}
